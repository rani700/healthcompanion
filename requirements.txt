# Full dev/test dependencies. Runtime-only deps live in requirements-runtime.txt
# (which the Docker image installs).
-r requirements-runtime.txt

# CLI + tests
typer>=0.12.0
pytest>=8.0.0
httpx>=0.27.0
